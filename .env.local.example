# .env.local.example
# Kopiere diese Datei zu .env.local für lokale Einstellungen
# .env.local wird NICHT in git committed und überschreibt .env

# Lokaler Default-Pfad für den File Browser
# RCLONE_GUI_DEFAULT_PATH=/home/myuser/Documents

# Lokale Entwicklungseinstellungen
# RUST_LOG=debug

# Lokale Bind-Adresse (falls abweichend)
# RCLONE_GUI_BIND=0.0.0.0:3000

# Weitere lokale Anpassungen...